# Deploys the cloud-mcp Cloud Run services, which run the PyAirbyte MCP server.
#
# The services themselves live in other repos, so this workflow only resolves
# which PyAirbyte build should ship and hands it off:
# - airbytehq/airbyte-ops-mcp - every deploy, preview and production, goes to
#   that repo's `Deploy MCP Server` workflow via `repository_dispatch`.
# - airbytehq/airbyte-platform-internal - a production deploy additionally goes
#   to that repo's `Deploy Cloud MCP` workflow via `workflow_dispatch`. That
#   repo has only a production service, so the preview path skips it.
#
# Triggers:
# - push (main) - Every merge refreshes cloud-mcp-preview with that commit,
#   installed straight from git so no PyPI release is needed.
# - workflow_call - `pypi_publish.yml` calls this after a publish to ship the
#   published version: every publish refreshes the preview service, and a
#   public release also goes to production.

name: Deploy Cloud MCP

on:
  push:
    branches:
      - main
  workflow_call:
    inputs:
      pyairbyte-version:
        description: 'Published PyAirbyte version to pin (e.g. "0.56.0").'
        required: true
        type: string
      preview:
        description: 'Whether to deploy the preview service instead of production ("true" or "false").'
        required: true
        type: string
    secrets:
      OCTAVIA_BOT_APP_ID:
        description: 'GitHub App ID used to dispatch the airbyte-ops-mcp deploy workflow.'
        required: true
      OCTAVIA_BOT_PRIVATE_KEY:
        description: 'Private key for the GitHub App above.'
        required: true

permissions:
  contents: read

# Serialize the dispatch requests per target service. The airbyte-ops-mcp
# rollout does not guard concurrency, so for that target this narrows the race
# rather than removing it; airbyte-platform-internal serializes its own deploys
# under its `deploy-cloud-mcp` group. Preview only ever wants the newest commit,
# so supersede in-flight preview runs; production runs to completion.
concurrency:
  group: deploy-cloud-mcp-${{ inputs.preview || 'true' }}
  cancel-in-progress: ${{ inputs.preview != 'false' }}

jobs:
  deploy_cloud_mcp:
    name: Deploy Cloud MCP
    runs-on: ubuntu-latest
    steps:
      - name: Resolve Variables
        id: resolve-vars
        env:
          PYAIRBYTE_VERSION: ${{ inputs.pyairbyte-version || '' }}
          # `preview` is a required `workflow_call` input, so a non-empty value
          # means a caller invoked this workflow. On the `push` trigger the
          # `inputs` context is empty, which is what selects the git install
          # below. This does not use `github.event_name`, which in a reusable
          # workflow reports the *caller's* trigger.
          PREVIEW: ${{ inputs.preview || '' }}
        run: |
          set -euo pipefail
          version="${PYAIRBYTE_VERSION#v}"

          # ---------- Validate inputs (guard statements) ----------
          if [ -n "$PREVIEW" ]; then
            printf '%s\n' true false | grep -qxF "$PREVIEW" \
              || { echo "::error::Invalid preview value: '$PREVIEW' (allowed: true, false)"; exit 1; }
            # A caller shipping a publish must name the published version, or
            # this would silently deploy the ref instead of the release.
            [ -n "$version" ] \
              || { echo "::error::pyairbyte-version is required when this workflow is called."; exit 1; }
          else
            PREVIEW=true
          fi
          if [ -n "$version" ]; then
            case "$version" in
              [!0-9]*|*[!0-9A-Za-z.+!-]*)
                echo "::error::Invalid PEP 440 version: $version"
                exit 1
                ;;
            esac
          fi

          # ---------- Requirement to install into the image ----------
          # A published version is pinned exactly; otherwise install the
          # triggering commit from git, so a merge to main needs no release.
          if [ -n "$version" ]; then
            requirement="airbyte==$version"
            pinned="true"
          else
            requirement="airbyte @ git+https://github.com/${GITHUB_REPOSITORY}@${GITHUB_SHA}"
            pinned="false"
          fi

          {
            printf 'requirement=%s\n' "$requirement"
            printf 'pinned=%s\n' "$pinned"
            printf 'preview=%s\n' "$PREVIEW"
          } | tee -a "$GITHUB_OUTPUT"

      - name: Install uv
        if: steps.resolve-vars.outputs.pinned == 'true'
        uses: astral-sh/setup-uv@f06b870e0a91d23284a3013acc55e6f88ab4b904 # v7
        with:
          enable-cache: false

      # Gate on `uv` itself: it reads the PEP 691 JSON variant of the simple
      # index, which PyPI caches separately (`vary: Accept`) from the HTML one
      # and which can stay stale for far longer. Polling anything else can go
      # green while the cloud-mcp image build still cannot resolve the version.
      - name: Wait for PyAirbyte version to be resolvable
        if: steps.resolve-vars.outputs.pinned == 'true'
        env:
          PYPI_REQUIREMENT: ${{ steps.resolve-vars.outputs.requirement }}
        run: |
          set -euo pipefail
          attempt=1
          while [ "$attempt" -le 30 ]; do
            if uv pip compile --quiet --no-cache --no-deps --python-version 3.12 \
              --output-file "$RUNNER_TEMP/pyairbyte-requirement.txt" - \
              <<<"$PYPI_REQUIREMENT"; then
              break
            fi
            echo "$PYPI_REQUIREMENT is not resolvable from PyPI yet (attempt $attempt/30)."
            if [ "$attempt" -lt 30 ]; then
              sleep 10
            fi
            attempt=$((attempt + 1))
          done
          [ "$attempt" -le 30 ] \
            || { echo "::error::$PYPI_REQUIREMENT did not become resolvable from PyPI after 30 attempts."; exit 1; }

      - name: Build cloud-mcp dispatch payload
        id: build-client-payload
        env:
          PYAIRBYTE_REQUIREMENT: ${{ steps.resolve-vars.outputs.requirement }}
          PREVIEW: ${{ steps.resolve-vars.outputs.preview }}
        run: |
          set -euo pipefail
          payload="$(jq -cn \
            --arg requirement "$PYAIRBYTE_REQUIREMENT" \
            --arg preview "$PREVIEW" \
            '{"mcp-server": "cloud-mcp", "preview": $preview, "cloud-mcp-pyairbyte-requirement": $requirement}')"
          echo "payload=$payload" | tee -a "$GITHUB_OUTPUT"

      - name: Authenticate as GitHub App
        id: get-app-token
        uses: actions/create-github-app-token@67018539274d69449ef7c02e8e71183d1719ab42 # v2.1.4
        with:
          owner: airbytehq
          repositories: airbyte-ops-mcp
          app-id: ${{ secrets.OCTAVIA_BOT_APP_ID }}
          private-key: ${{ secrets.OCTAVIA_BOT_PRIVATE_KEY }}
          # `repository_dispatch` needs contents:write and nothing else.
          permission-contents: write

      - name: Trigger cloud-mcp deploy
        uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4.0.1
        with:
          token: ${{ steps.get-app-token.outputs.token }}
          repository: airbytehq/airbyte-ops-mcp
          event-type: deploy-cloud-mcp
          client-payload: ${{ steps.build-client-payload.outputs.payload }}

      # ---------- Second target: airbyte-platform-internal ----------
      # Its `Deploy Cloud MCP` workflow builds the image from a vendored copy
      # of the cloud-mcp build context and rolls out the Cloud Run service it
      # owns. It is `workflow_dispatch`-only and exposes typed inputs, so this
      # posts to the dispatches endpoint rather than sending a
      # `repository_dispatch` event as the ops-mcp handoff above does.
      #
      # Gated on the production path: that repo has no preview service, so a
      # preview deploy has nothing to target there.

      - name: Authenticate as GitHub App for platform-internal
        id: get-internal-app-token
        if: steps.resolve-vars.outputs.preview == 'false'
        uses: actions/create-github-app-token@67018539274d69449ef7c02e8e71183d1719ab42 # v2.1.4
        with:
          owner: airbytehq
          repositories: airbyte-platform-internal
          app-id: ${{ secrets.OCTAVIA_BOT_APP_ID }}
          private-key: ${{ secrets.OCTAVIA_BOT_PRIVATE_KEY }}
          # A `workflow_dispatch` through the REST API needs actions:write and
          # nothing else. Note this differs from the contents:write the
          # `repository_dispatch` above requires.
          permission-actions: write

      - name: Trigger platform-internal cloud-mcp deploy
        id: dispatch-internal
        if: steps.resolve-vars.outputs.preview == 'false'
        env:
          GH_TOKEN: ${{ steps.get-internal-app-token.outputs.token }}
          PYAIRBYTE_REQUIREMENT: ${{ steps.resolve-vars.outputs.requirement }}
        run: |
          set -euo pipefail

          # Reaching here means `preview=false`, which only a caller can set and
          # which the guards above pair with a non-empty version, so the
          # requirement is always the pinned `airbyte==<version>` form.
          version="${PYAIRBYTE_REQUIREMENT#airbyte==}"

          # The target defaults `image_tag` to `manual-<its own short sha>`,
          # which names nothing about the PyAirbyte build and repeats across
          # releases whenever that repo's HEAD has not moved. Name the tag after
          # the version instead. PEP 440 allows `+` and `!`, which are not legal
          # in a Docker tag and which the target rejects outright, so map every
          # disallowed character to `-`.
          image_tag="pyairbyte-$(printf '%s' "$version" | sed 's/[^a-zA-Z0-9._-]/-/g')"

          # `ref` selects the copy of the workflow that runs, and the build
          # context it vendors, so pin it to that repo's default branch rather
          # than to whatever ref triggered this run here.
          #
          # `deploy` is declared as a boolean there; the API coerces the string
          # to the declared type, which is also how that repo's own
          # `gh workflow run --raw-field` dispatches pass it.
          jq -cn \
            --arg requirement "$PYAIRBYTE_REQUIREMENT" \
            --arg image_tag "$image_tag" \
            '{
              "ref": "master",
              "inputs": {
                "pyairbyte_requirement": $requirement,
                "image_tag": $image_tag,
                "deploy": "true"
              }
            }' \
            | gh api --method POST \
              --header 'Accept: application/vnd.github+json' \
              '/repos/airbytehq/airbyte-platform-internal/actions/workflows/deploy-cloud-mcp.yml/dispatches' \
              --input -

          printf 'image_tag=%s\n' "$image_tag" | tee -a "$GITHUB_OUTPUT"

      - name: Summarize dispatch
        env:
          PYAIRBYTE_REQUIREMENT: ${{ steps.resolve-vars.outputs.requirement }}
          PREVIEW: ${{ steps.resolve-vars.outputs.preview }}
          # Empty whenever the platform-internal dispatch was skipped.
          INTERNAL_IMAGE_TAG: ${{ steps.dispatch-internal.outputs.image_tag }}
        run: |
          {
            printf 'Requested a cloud-mcp deploy in airbytehq/airbyte-ops-mcp.\n'
            printf 'Requirement: %s\n' "$PYAIRBYTE_REQUIREMENT"
            printf 'Preview: %s\n' "$PREVIEW"
            if [ -n "$INTERNAL_IMAGE_TAG" ]; then
              printf 'Requested a cloud-mcp deploy in airbytehq/airbyte-platform-internal (image tag: %s).\n' "$INTERNAL_IMAGE_TAG"
            else
              printf 'Skipped airbytehq/airbyte-platform-internal, which has no preview service.\n'
            fi
          } | tee -a "$GITHUB_STEP_SUMMARY"
